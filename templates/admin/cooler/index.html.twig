{% extends 'base.html.twig' %}

{% block title %}Gestion des systèmes de refroidissement - Admin{% endblock %}

{% block main %}
	<main class="main-show">
{% include "admin/part/adminNav.html.twig" %}
		<h1>Gestion des systèmes de refroidissement</h1>
		<a href="{{ path('app_admin_cooler_new') }}" class="btn">Créer un nouveau système de refroidissement</a>
		<div class="table-div">
			<table class="table">
				<thead>
					<tr>
						<th>ID</th>
						<th>Marque</th>
						<th>Modèle</th>
						<th>Prix (€)</th>
						<th>Actions</th>
					</tr>
				</thead>
				<tbody>
					{% for cooler in coolers %}
						<tr>
							<td>{{ cooler.id }}</td>
							<td>{{ cooler.marque }}</td>
							<td>{{ cooler.modele }}</td>
							<td>{{ cooler.prix }}</td>
							<td>
								<a href="{{ path('app_admin_cooler_edit', {'id': cooler.id}) }}" aria-label="Modifier {{ cooler.marque }} {{ cooler.modele }}">
									<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewbox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-pencil-icon lucide-pencil" role="img" aria-hidden="true"><path d="M21.174 6.812a1 1 0 0 0-3.986-3.987L3.842 16.174a2 2 0 0 0-.5.83l-1.321 4.352a.5.5 0 0 0 .623.622l4.353-1.32a2 2 0 0 0 .83-.497z"/><path d="m15 5 4 4"/></svg>
								</a>
								<form method="post" action="{{ path('app_admin_cooler_delete', {'id': cooler.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce système de refroidissement ?');" style="display:inline;">
									<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cooler.id) }}">
									<button class="btn" style="background:none;border:none;padding:0;" aria-label="Supprimer {{ cooler.marque }} {{ cooler.modele }}">
										<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#e5676e" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-trash2-icon lucide-trash-2" role="img" aria-hidden="true"><path d="M10 11v6"/><path d="M14 11v6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/><path d="M3 6h18"/><path d="M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
									</button>
								</form>
							</td>
						</tr>
					{% else %}
						<tr>
							<td colspan="5">Aucun système de refroidissement trouvé</td>
						</tr>
					{% endfor %}
				</tbody>
			</table>
		</div>
	</main>
{% endblock %}
