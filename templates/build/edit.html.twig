{% extends 'base.html.twig' %}

{% block title %}Modifier le build
{% endblock %}

{% block main %}
	<main class="main-form">
		<section>
			<h1>Modifier le build</h1>

			{{ include('build/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
			<div>
				{% if is_granted('ROLE_ADMIN') %}
					<a href="{{ path('app_build_index') }}">retour à la liste</a>
				{% endif %}
				{{ include('build/_delete_form.html.twig') }}
			</div>
		</section>
	</main>
{% endblock %}
