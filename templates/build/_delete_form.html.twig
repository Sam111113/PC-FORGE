<form method="post" action="{{ path('app_build_delete', {'id': build.id}) }}" onsubmit="return confirm('Est-tu sûr de vouloir supprimer ce build ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ build.id) }}">
    <button class="btn">Supprimer</button>
</form>
