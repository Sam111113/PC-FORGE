<form method="post" action="{{ path('app_boitier_delete', {'id': boitier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boitier.id) }}">
    <button class="btn">Supprimer</button>
</form>
