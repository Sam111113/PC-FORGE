<form method="post" action="{{ path('app_fan_delete', {'id': fan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fan.id) }}">
    <button class="btn">supprimer</button>
</form>
